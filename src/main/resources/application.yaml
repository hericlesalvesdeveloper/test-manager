spring:
  application:
    name: TestManager

  datasource:
    url: ${DATABASE_URL}
    username: ${USER}
    password: ${PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
