spring:
  application:
    name: TestManager

  datasource:
    url: jdbc:postgresql://localhost:5432/testManager
    username: postgres
    password: gato$2026

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
